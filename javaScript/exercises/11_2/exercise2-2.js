function toUpperCase (str) {
  return str.toUpperCase();
};

function firstLetter(str) {
  return str[0];
}

function strConcatenate (str1, str2) {
  return str1 + str2;
}

module.exports = { toUpperCase, firstLetter, strConcatenate }